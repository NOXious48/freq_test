# ============================================================
# MFAD_dev — Deep Sentinel .gitignore
# ============================================================

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
.eggs/
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Conda / Virtual Environments ─────────────────────────────
.env
.venv
env/
venv/
ENV/
conda-env/

# ── Jupyter ───────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Models (large binary files — do not commit) ──────────────
models/
*.pth
*.pt
*.ckpt
*.bin
*.onnx
*.pkl
*.h5
*.hdf5
*.safetensors

# ── Datasets (never commit raw data) ─────────────────────────
data/
test_images/
*.jpg
*.jpeg
*.png
*.bmp
*.webp
*.mp4
*.avi
*.mov

# ── Submodules (cloned repos — not your code) ────────────────
# These are tracked as git submodules or excluded entirely.
# If using git submodule add, remove these lines.
submodules/GANDCTAnalysis/
submodules/frequency-forensics/
submodules/FreqNet-DeepfakeDetection/
submodules/NPR-DeepfakeDetection/

# ── Generated outputs (runtime artifacts) ────────────────────
reports/
logs/
temp/

# ── Calibration outputs ───────────────────────────────────────
config_calibrated.json

# ── OS files ─────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── IDEs ─────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Secrets / Keys ───────────────────────────────────────────
.env
*.env
secrets.json
api_keys.json

# ============================================================
# WHAT GETS COMMITTED (everything not listed above):
#
# ✅ agents/frequency_agent.py
# ✅ agents/__init__.py
# ✅ pipeline/langchain_tools.py
# ✅ scripts/batch_run.py
# ✅ scripts/calibrate_dct.py
# ✅ scripts/compress_image.py
# ✅ scripts/evaluate_frequency_agent.py
# ✅ scripts/generate_test_images.py
# ✅ references/*.md          (all reference docs)
# ✅ config.json              (thresholds only, no secrets)
# ✅ submodules/CONFLICTS.md  (dependency conflict log)
# ✅ FREQUENCY_AGENT_SETUP_GEMINI.md
# ✅ PROGRESS_HANDOFF.md
# ✅ .gitignore               (this file)
# ✅ README.md                (create before pushing)
#
# ❌ models/                  (too large)
# ❌ data/                    (dataset — too large)
# ❌ test_images/             (images — not code)
# ❌ reports/                 (generated at runtime)
# ❌ logs/                    (generated at runtime)
# ❌ submodules/*/            (third-party repos)
# ❌ config_calibrated.json   (generated at runtime)
# ============================================================
